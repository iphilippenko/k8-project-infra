apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base

patches:
  - path: values-patch.yaml
    target:
      kind: HelmRelease
      name: bookmarks-app
